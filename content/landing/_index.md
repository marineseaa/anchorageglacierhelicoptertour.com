---
# The /landing/ section page is an auto-generated near-empty list that would
# ship thin content into the sitemap. Suppress the section page itself; the
# spokes below it render normally at their own top-level slugs, and
# explore-hub.html finds them via `where .Site.RegularPages "Section" "landing"`,
# which is unaffected by this. (Codified 2026-07-11.)
title: "Landing"
build:
  render: never
  list: never
---
